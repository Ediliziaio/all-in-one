# ── Studentato Napoleone Padova — Cloudflare Pages / Netlify _redirects ──
# Nota: Cloudflare gestisce HTTPS automaticamente, non serve regola HTTP→HTTPS

# www → non-www (301 permanente — elimina contenuto duplicato per SEO)
https://www.studentatonapoleone.com/*  https://studentatonapoleone.com/:splat  301

# SPA fallback — tutte le rotte React Router → index.html
/*    /index.html   200
